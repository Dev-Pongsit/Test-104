---
import ContactCTA from '../components/ContactCTA.astro';
import Hero from '../components/Hero.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About | Jeanine White" description="About Jeanine White Lorem Ipsum">
  <div class="stack gap-20">
    <main class="wrapper about">
      <Hero
        title="Haus48 Coffee"
        tagline="Haus48 Coffee ก้าวเข้าสู่ปีที่ 3 แล้วครับ ✨☕"
      >
        <img
          width="1553"
          height="873"
          src="/Test-104/assets/494539391_673381512289784_7419526062699911065_n.jpg"
          alt="Haus48 Coffee"
        />
      </Hero>

      <section>
        <h2 class="section-title">About</h2>
        <div class="content">
          <p>
            Haus48 Coffee ก้าวเข้าสู่ปีที่ 3 แล้วครับ ✨☕
            จากร้านกาแฟเล็ก ๆ ที่ชาญเมือง ของเมืองบึงกาฬ…
            เรายังจำวันแรกที่เปิดร้านได้ดี ความตื่นเต้น ความกังวล และความตั้งใจเต็มเปี่ยม
            เราฝันอยากให้ที่นี่เป็น “บ้านหลังที่สอง” สำหรับทุกคน
            ที่ใครผ่านมาก็แวะพักได้ มีกาแฟดี ๆ ขนมอร่อย ๆ และบรรยากาศที่อบอุ่นใจ 🏡
          </p>
          <p>-----</p>
          <p>ทุกวัน พิกัดเลยห้างบิ๊กเสือ 200 เมตร</p>
          <p>🖤Open Daily 08.30 - 17.30 💨</p>
          <p>ตรงข้ามประเสริฐนิกรกุล</p>
          <p>📍HAUS48 CAFE</p>
        </div>
      </section>
    </main>

    <!-- <ContactCTA /> -->
  </div>
</BaseLayout>

<style>
  /* Layout */
  .stack {
    display: flex;
    flex-direction: column;
  }

  .gap-20 {
    gap: 5rem; /* ปรับช่องว่างตามต้องการ */
  }

  .wrapper {
    padding: 2rem;
  }

  /* About Section */
  .about {
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
  }

  img {
    margin-top: 1.5rem;
    border-radius: 1.5rem;
    box-shadow: var(--shadow-md);
    max-width: 100%;
    height: auto;
  }

  section {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    color: var(--gray-200);
  }

  .section-title {
    font-size: var(--text-xl);
    color: var(--gray-0);
    margin-bottom: 0.5rem;
  }

  .content {
    /* ถ้าต้องการให้เนื้อหากว้างกว่าปกติ สามารถปรับได้ */
    max-width: 700px;
  }

  /* ลิงก์ภายในเนื้อหา */
  .content a {
    text-decoration: underline;
    text-underline-offset: 0.25em;
    transition: text-decoration-color 0.3s ease;
    color: inherit;
  }

  .content a:hover,
  .content a:focus {
    text-decoration-color: currentColor;
  }

  /* Responsive */
  @media (min-width: 50em) {
    .about {
      display: grid;
      grid-template-columns: 1fr 60% 1fr;
    }

    /* เลือกองค์ประกอบลูกตัวแรกของ .about ให้เริ่มที่ column 2 */
    .about > :first-child {
      grid-column-start: 2;
    }

    section {
      font-size: var(--text-lg);
      /* ยกเลิก display:flex บน desktop */
      display: block;
    }
  }
</style>
